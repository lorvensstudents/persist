<%= form_with(model: bright, local: true) do |form| %>
  <% if bright.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bright.errors.count, "error") %> prohibited this bright from being saved:</h2>

      <ul>
      <% bright.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :bright_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age, id: :bright_age %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :bright_address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :bright_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
